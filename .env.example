# Build-time public Google web OAuth client ID. No secrets belong in VITE_* variables.
VITE_GOOGLE_CLIENT_ID=
# Development proxy only. Production uses same-origin Nginx.
API_PROXY_TARGET=http://localhost:8080
