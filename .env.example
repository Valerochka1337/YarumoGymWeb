# Optional local fallback. Production reads the public Google web client ID from its backend.
VITE_GOOGLE_CLIENT_ID=
# Development proxy only. Production uses same-origin Nginx.
API_PROXY_TARGET=http://localhost:8080
