import { describe, expect, it } from "vitest";
import { newTrial, pending, remainingRest, trialReducer } from "./trialReducer";
import type { Preview } from "./types";
const preview: Preview = { title:"Test", estimatedDurationSeconds:60, exercises:[{exerciseKey:"x",name:"Press",type:"STRENGTH",restSeconds:30,sets:[{weightKg:20,reps:8,durationSec:null,speedKmh:null,inclinePct:null}]}] };
describe("trial reducer", () => { it("starts only on explicit action and keeps plan immutable", () => { let state=newTrial("a".repeat(43),preview,100); expect(state.startedAt).toBe(0); state=trialReducer(state,{type:"start",now:150}); expect(state.startedAt).toBe(150); state=trialReducer(state,{type:"actual",exerciseIndex:0,setIndex:0,actual:{weightKg:25,reps:9,durationSec:null,speedKmh:null,inclinePct:null}}); state=trialReducer(state,{type:"complete",exerciseIndex:0,setIndex:0,now:200}); expect(state.preview.exercises[0].sets[0].weightKg).toBe(20); expect(remainingRest(state.restDeadlineAt,1000)).toBe(30); state=trialReducer(state,{type:"finish",now:2000}); expect(pending(state).completedSets).toHaveLength(1); }); });
it("initializes visible planned facts and blocks zero or invalid completion", () => { let state=newTrial("a".repeat(43),preview,100); state=trialReducer(state,{type:"start",now:100}); expect(state.exercises[0][0].actual).toMatchObject({weightKg:20,reps:8}); state=trialReducer(state,{type:"complete",exerciseIndex:0,setIndex:0,now:200}); expect(state.exercises[0][0].completed).toBe(true); state=trialReducer(state,{type:"complete",exerciseIndex:0,setIndex:0,now:300}); expect(state.restDeadlineAt).toBeUndefined(); state=trialReducer(newTrial("b".repeat(43),preview,100),{type:"finish",now:200}); expect(state.phase).toBe("preview"); expect(state.error).toMatch(/хотя бы один/); });
